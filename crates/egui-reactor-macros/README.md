# egui-reactor-macros

Procedural macros for [egui-reactor](https://crates.io/crates/egui-reactor): `rsx!`, `#[component]` and `#[hook]`.

Do not depend on this crate directly. `egui_reactor::prelude` re-exports all three.

Docs: https://fand.github.io/egui-reactor/
