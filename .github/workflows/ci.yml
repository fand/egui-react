name: CI

on:
  push:
    branches: [main]
  pull_request:

# A newer push to the same branch supersedes the run in flight: the old one is
# only ever going to report on a commit nobody is looking at any more.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  # Incremental artifacts double the size of `target/`; CI never reuses them.
  CARGO_INCREMENTAL: 0
  # Debug info is most of the link time — and most of the bytes — of a test
  # binary with egui in it, and nothing here reads a backtrace. Set through the
  # environment rather than `[profile.dev]` so a local `cargo test` keeps it.
  CARGO_PROFILE_DEV_DEBUG: none
  CARGO_PROFILE_TEST_DEBUG: none

# The two halves share no build artifacts — clippy, the test binaries and the
# wasm builds each compile the workspace their own way — so running them as one
# job only added their times together.
jobs:
  check:
    name: fmt / clippy / test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # There used to be a step here deleting the dotnet / android / ghc
      # toolchains, from a time `ld` died with a bus error and the runner was
      # read as having ~14 GB. It has 145 GB: the step's own `df -h` printed
      # 107 GB still free after it ran, so it was buying ~17 GB of headroom on
      # top of ~90 GB for 30-85 s a run. Whatever that bus error was, disk was
      # not it — and a test binary is a fraction of its old size now that the
      # dev profile carries no debug info.

      # eframe (pulled in by the examples, which `cargo test --workspace`
      # builds) needs the X11 / Wayland / GTK development headers on Linux.
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y \
            libgtk-3-dev \
            libxkbcommon-dev \
            libwayland-dev \
            libxcb-render0-dev \
            libxcb-shape0-dev \
            libxcb-xfixes0-dev \
            libxkbcommon-x11-dev \
            libssl-dev

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.95"
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown

      # rust-cache's key hashes `rustc -vV` of every toolchain `rustup toolchain
      # list` knows, the runner image's own `stable` included. That compiler
      # builds nothing here, but its version moves with every image rollout —
      # 1.98.0 to 1.98.1 on 2026-09-08 — and while a rollout is under way the
      # fleet is mixed, so the key depended on which runner a job landed on and
      # each variant had to be warmed separately. Leave rustup knowing only the
      # pinned toolchain, and the key depends only on things this repo controls.
      - name: Forget the runner's own toolchains
        run: |
          active=$(rustup show active-toolchain | cut -d' ' -f1)
          rustup toolchain list | cut -d' ' -f1 | grep -vx "$active" | xargs -r -n1 rustup toolchain uninstall
          rustup toolchain list

      - uses: Swatinem/rust-cache@v2
        with:
          # A red run is the one most likely to be pushed to again in a minute.
          # Without this its dependency builds are thrown away.
          cache-on-failure: true

      - name: cargo fmt
        run: cargo fmt --all --check

      - name: cargo clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: cargo test
        run: cargo test --workspace
      # Without egui's embedded fonts (`egui-react-app` taken with
      # `default-features = false`): the resolver must not hand epaint a key
      # with no bytes behind it.
      - name: cargo test (no default fonts)
        run: cargo test -p egui-react-app --no-default-features

  wasm:
    name: wasm / trunk / site
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # No X11 / Wayland / GTK headers here: nothing in the wasm dependency
      # graph reaches for them, and this job never builds for the host.

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.95"
          targets: wasm32-unknown-unknown

      # Same reason as in `check`: keep the image's own Rust out of the cache key.
      - name: Forget the runner's own toolchains
        run: |
          active=$(rustup show active-toolchain | cut -d' ' -f1)
          rustup toolchain list | cut -d' ' -f1 | grep -vx "$active" | xargs -r -n1 rustup toolchain uninstall
          rustup toolchain list

      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: cargo check (wasm)
        run: cargo check --workspace --target wasm32-unknown-unknown

      - uses: jetli/trunk-action@v0.5.1
        with:
          version: latest

      # `data-wasm-opt="z"` costs wasm-opt 25-40 s per example — three quarters
      # of this job — to shrink a `.wasm` that CI only ever checks the
      # existence of. `0` is the one level trunk answers by not running
      # wasm-opt at all. pages.yml and the Cloudflare PR preview still build
      # the gallery optimised, so the optimised path is covered on every PR.
      #
      # `embed.html` is named separately because the glob only matches
      # `index.html`, and the embed is one more build to keep cheap.
      - name: Turn off wasm-opt for the build check
        run: |
          sed -i 's/data-wasm-opt="[^"]*"/data-wasm-opt="0"/' \
            examples/*/index.html examples/gallery/embed.html

      # `trunk build <path>` resolves the cargo root package from the current
      # directory, so the example's Trunk.toml is passed instead.
      #
      # Every example, the gallery included. `fetch` is also the only check
      # that the wasm `spawn_local` path of `use_future` really links.
      - name: trunk build (every example)
        run: |
          for config in examples/*/Trunk.toml; do
            echo "==> $config"
            trunk build --release --config "$config"
          done

      # The site's iframe wasm. The loop's glob only matches `Trunk.toml`, and
      # this one is `Trunk-embed.toml`, so it gets a line of its own.
      - name: trunk build (the site's embed)
        run: trunk build --release --config examples/gallery/Trunk-embed.toml

      # The site build is the only thing that catches a dead link, a renamed
      # example page or a broken `::: example-source` include, and it takes a
      # few seconds. `cargo doc` is not run here: a doc warning is not worth a
      # third toolchain step on every PR.
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: site/package-lock.json

      - name: vitepress build (site)
        run: |
          npm --prefix site ci
          cd site && npx vitepress build

# The pixel snapshot tests (`cargo test -p egui-react-elements --features
# snapshot` and `cargo test -p gallery --features snapshot`) are deliberately
# not run here: the committed images are rendered on macOS and would not match
# this runner's software renderer. See the README.
